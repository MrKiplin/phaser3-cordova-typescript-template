{
  "name": "phaser3-typescript-cordova-template",
  "version": "1.0.0",
  "description": "Template for multi-platform (mobile, tablet, browser etc.) games using Phaser3 and TypeScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p ./tsconfig.build.json && cp -r ./src/index.html www/"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Theodore J H Jones <https://github.com/MrKiplin>",
  "license": "MIT",
  "devDependencies": {
    "cordova-plugin-whitelist": "^1.3.4",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "phaser": "^3.24.1"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    }
  }
}
